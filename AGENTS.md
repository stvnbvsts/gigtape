# Gigtape Agent Guide

Shared agent instructions and reusable skills live in `.agents/`.

Start with `.agents/AGENTS.md`, then use `.agents/skills/` for reusable
workflows. Tool-specific folders should only contain local settings or adapter
notes.

