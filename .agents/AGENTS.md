# Gigtape Agent Notes

Shared skills live in `.agents/skills/`.

Claude-specific configuration lives in `.claude/`. Keep reusable instructions,
skills, prompts, and long-lived project context in `.agents/` so they can be
used by multiple coding agents.

